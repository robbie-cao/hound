#include <stdio.h>#include <stdlib.h>#include <stdint.h>#define MAX     1000#define FALSE   0#define TRUE    1uint64_t sFibArr[MAX];uint64_t fib(uint32_t n){    if (n == 0 || n == 1) {        sFibArr[n] = 1;        return 1;    }    if (n >= MAX) {        printf("Out of range!\n");        return (uint64_t)(-1);    }    if (sFibArr[n]) {        return sFibArr[n];    }    sFibArr[n] = fib(n - 1) + fib(n - 2);    return sFibArr[n];}int main(int argc, char *argv[]){    int n = 0;    uint64_t res = 0;    if (argc != 2) {        printf("%s <n>\n", argv[0]);    }    n = atoi(argv[1]);    res = fib(n);    for (int i = 0; i < n; i++) {        printf("Fib(%d) = %lld\n", i, fib(i));    }    return 0;}